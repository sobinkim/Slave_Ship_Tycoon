# Basic Entity Sample

Add `SampleEntity` to a GameObject, then add `SampleMovementComponent` on the same object or a child.

Add `SampleEventUsage` anywhere in the scene and press Space during Play Mode to raise and receive a sample event.
